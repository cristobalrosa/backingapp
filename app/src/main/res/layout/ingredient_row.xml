<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="5dip" >
    <TextView
        android:id="@+id/ingredient_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textStyle="bold"
        android:text="@string/example_ingredient_name"
        android:textSize="17dp" />

    <TextView
        android:id="@+id/ingredient_quantity"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/ingredient_name"
        android:layout_alignBottom="@+id/ingredient_name"
        android:layout_marginStart="100dp"
        android:layout_marginBottom="2dp"
        android:layout_toEndOf="@+id/ingredient_name"
        android:text="@string/example_ingredient_quantity"
        android:textColor="#343434"
        android:textSize="14dp" />

    <TextView
        android:id="@+id/ingredient_measure"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/ingredient_name"
        android:layout_alignBottom="@+id/ingredient_name"
        android:layout_toEndOf="@+id/ingredient_quantity"
        android:layout_marginStart="5dp"
        android:text="@string/example_ingredient_measure"
        android:textColor="#343434"
        android:textSize="14dp" />
</RelativeLayout>
